const sum = (a, b) => a + b;
const mul = (a, b) => a * b;
const sub = (a, b) => a - b;
const div = (a, b) => a / b;
const calculatePercentage = (num, percentage) => (num*percentage)/100

module.exports = { sum, mul, sub, div };